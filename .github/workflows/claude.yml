name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  detect-non-claude-review:
    name: Skip Exact @claude Review Command
    permissions: {}
    runs-on: ubuntu-latest
    outputs:
      run_claude: ${{ steps.check.outputs.run_claude }}
    steps:
      - name: Skip comments with @claude review on its own line
        id: check
        env:
          EVENT_NAME: ${{ github.event_name }}
          COMMENT_BODY: ${{ github.event.comment.body }}
        run: |
          if [ "$EVENT_NAME" != "issue_comment" ]; then
            echo "run_claude=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          has_review_command="$(python - <<'PY'
          import os

          body = os.environ.get("COMMENT_BODY", "")
          print(
              "true"
              if any(line.strip() == "@claude review" for line in body.splitlines())
              else "false"
          )
          PY
          )"
          if [ "$has_review_command" = "true" ]; then
            echo "run_claude=false" >> "$GITHUB_OUTPUT"
          else
            echo "run_claude=true" >> "$GITHUB_OUTPUT"
          fi

  claude:
    name: Run Claude Code (Non-Review)
    needs: [detect-non-claude-review]
    if: |
      always() &&
      (
        (
          github.event_name == 'issue_comment' &&
          contains(github.event.comment.body, '@claude') &&
          (!github.event.issue.pull_request ||
          needs.detect-non-claude-review.outputs.run_claude == 'true') &&
          contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
        ) ||
        (
          github.event_name == 'pull_request_review_comment' &&
          contains(github.event.comment.body, '@claude') &&
          contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
        ) ||
        (
          github.event_name == 'pull_request_review' &&
          contains(github.event.review.body, '@claude') &&
          contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.review.author_association)
        ) ||
        (
          github.event_name == 'issues' &&
          (
            contains(github.event.issue.body, '@claude') ||
            contains(github.event.issue.title, '@claude')
          ) &&
          contains(fromJson('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association)
        )
      )
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
      actions: read # Required for Claude to read CI results on PRs
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run Claude Code
        id: claude
        uses: anthropics/claude-code-action@b47fd721da662d48c5680e154ad16a73ed74d2e0
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          additional_permissions: |
            actions: read

          # Tools based on CLAUDE.md development commands
          claude_args: |
            --allowed-tools "Bash(uv run pytest:*),Bash(uv run alembic:*),Bash(uv run python:*),Bash(uv sync:*),Bash(pnpm install:*),Bash(pnpm check:*),Bash(pnpm lint:*),Bash(pnpm format:*),Bash(pnpm test:*),Bash(pnpm build:*),Bash(gh issue:*),Bash(gh pr:*),Bash(gh search:*)"
